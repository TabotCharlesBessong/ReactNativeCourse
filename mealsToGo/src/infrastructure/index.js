import { colors } from "./theme/colors";
import { space, lineHeights } from "./theme/spacing";
import { sizes } from "./theme/sizes";
import { fonts, fontWeights, fontSizes } from "./theme/fonts";

export const theme = {
	colors,
	space,
	lineHeights,
	sizes,
	fonts,
	fontSizes,
	fontWeights,
};
